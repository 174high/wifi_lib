
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src)

set(CMAKE_C_FLAGS "-shared -g -DDEBUG")

SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)

SET(DIR_TEST1_SRCS control_wifi.c)       

ADD_LIBRARY ( Test SHARED ${DIR_TEST1_SRCS})

